#!/bin/bash

# Install dotfiles
if [ ! -d $HOME/.dotfiles ]; then
  git clone https://github.com/mikemackintosh/dotfiles $HOME/.dotfiles
else
  cd $HOME/.dotfiles
  git pull --rebase
fi

# Create private dir
if [ -d "${HOME}/.private/" ]; then
  mkdir $HOME/.private
  touch $HOME/.private/nil
fi

# Backup
if [ -f $HOME/.bash_profile ]; then
  cp $HOME/.bash_profile $HOME/.bash_profile.orig
fi
if [ -f $HOME/.atom ]; then
  cp $HOME/.atom $HOME/.atom.orig
fi
if [ -f $HOME/.gitconfig ]; then
  cp $HOME/.gitconfig $HOME/.gitconfig.orig
fi
if [ -f $HOME/.vim ]; then
  cp $HOME/.vim $HOME/.vim.orig
fi
if [ -f $HOME/.vimrc ]; then
  cp $HOME/.vimrc $HOME/.vimrc.orig
fi
if [ -f $HOME/.curlrc ]; then
  cp $HOME/curlrc $HOME/.curlrc.org
fi
# Get readlink
`readlink $HOME/.bash_profile | grep .dotfiles/bash_profile.sh > /dev/null`
BASH=$?
if [ $BASH == 1 ]; then
  ln -s $HOME/.dotfiles/bash_profile.sh $HOME/.bash_profile
  ln -s $HOME/.dotfiles/atom $HOME/.atom
  ln -s $HOME/.dotfiles/gitconfig $HOME/.gitconfig
  ln -s $HOME/.dotfiles/vim $HOME/.vim
  ln -s $HOME/.vim/vimrc $HOME/.vimrc
  ln -s $HOME/.dotfiles/curlrc $HOME/.curlrc
fi
